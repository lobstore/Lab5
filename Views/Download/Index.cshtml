@{
    ViewData["Title"] = "Download Page";
}

<h1>Download Page</h1>

<div class="row">
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Лабораторная 1</h5>
                <a href="@Url.Action("Download", "Download", new { fileName = "IST-232m_Malkov_TA_LR1.zip" })" class="btn btn-primary">Download Lab 1</a>
                <button class="btn btn-primary" onclick="previewPDF('@Url.Action("Preview", "Download", new { fileName = "IST-232m_Malkov_TA_LR1.pdf" })')">Preview</button>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Лабораторная 2</h5>
                <a href="@Url.Action("Download", "Download", new { fileName = "IST-232m_Malkov_TA_LR2.zip" })" class="btn btn-primary">Download Lab 2</a>
                <button class="btn btn-primary" onclick="previewPDF('@Url.Action("Preview", "Download", new { fileName = "IST-232m_Malkov_TA_LR2.pdf" })')">Preview</button>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Лабораторная 3</h5>
                <a href="@Url.Action("Download", "Download", new { fileName = "IST-232m_Malkov_TA_LR3.zip" })" class="btn btn-primary">Download Lab 3</a>
                <button class="btn btn-primary" onclick="previewPDF('@Url.Action("Preview", "Download", new { fileName = "IST-232m_Malkov_TA_LR3.pdf" })')">Preview</button>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Лабораторная 4</h5>
                <a href="@Url.Action("Download", "Download", new { fileName = "IST-232m_Malkov_TA_LR4.zip" })" class="btn btn-primary">Download Lab 4</a>
                <button class="btn btn-primary" onclick="previewPDF('@Url.Action("Preview", "Download", new { fileName = "IST-232m_Malkov_TA_LR4.pdf" })')">Preview</button>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Лабораторная 5</h5>
                <a href="@Url.Action("Download", "Download", new { fileName = "IST-232m_Malkov_TA_LR5.zip" })" class="btn btn-primary">Download Lab 5</a>
                <button class="btn btn-primary" onclick="previewPDF('@Url.Action("Preview", "Download", new { fileName = "IST-232m_Malkov_TA_LR5.pdf" })')">Preview</button>
            </div>
        </div>
    </div>
    <!-- Остальные карточки для остальных лабораторных работ -->
</div>

<div class="preview-container" id="previewContainer">
    <div class="preview-content">
        <div id="pageViewer" class="pdfViewer"></div>
    </div>
    <button class="btn btn-primary close-button" onclick="closePreview()" id="closeButton">Close Preview</button>
</div>

<style>
    .card {
        border-color: #007bff;
    }

    .card-title {
        color: #007bff;
        font-size: 18px;
    }

    .btn-primary {
        background-color: #007bff;
        border-color: #007bff;
        padding: 10px 10px 10px 10px;
        margin-top:10px;
    }

        .btn-primary:hover {
            background-color: #0069d9;
            border-color: #0062cc;
        }

    .preview-container {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.7);
        display: none;
        align-items: center;
        justify-content: center;
        z-index: 9999;
        overflow: auto;
    }

    .preview-content {
        max-width: 80%;
        max-height: 80%;
        margin: 20px;
    }

    .pdfViewer {
        width: 100%;
        height: 100%;
    }

    .close-button {
        position: absolute;
        top: 20px;
        right: 20px;
    }
</style>

<script src="https://mozilla.github.io/pdf.js/build/pdf.js"></script>
<script>
    function previewPDF(url) {
        var container = document.getElementById("pageViewer");

        // Очищаем предыдущее содержимое превью
        container.innerHTML = "";

        // Загружаем и отображаем PDF файл
        var loadingTask = pdfjsLib.getDocument(url);
        loadingTask.promise.then(function (pdf) {
            var pagesPromises = [];

            for (var pageNum = 1; pageNum <= pdf.numPages; pageNum++) {
                pagesPromises.push(getPage(pageNum, pdf));
            }

            Promise.all(pagesPromises)
                .then(function (pages) {
                    pages.forEach(function (page) {
                        container.appendChild(page);
                    });

                    // Показываем контейнер превью
                    var previewContainer = document.getElementById("previewContainer");
                    previewContainer.style.display = "flex";
                })
                .catch(function (error) {
                    console.error(error);
                });
        });
    }

    function getPage(pageNum, pdf) {
        return new Promise(function (resolve, reject) {
            pdf.getPage(pageNum).then(function (page) {
                var viewport = page.getViewport({ scale: 1.5 });
                var canvas = document.createElement("canvas");
                var context = canvas.getContext("2d");
                canvas.height = viewport.height;
                canvas.width = viewport.width;

                var renderContext = {
                    canvasContext: context,
                    viewport: viewport,
                };

                page.render(renderContext).promise.then(function () {
                    resolve(canvas);
                });
            });
        });
    }

    function closePreview() {
        // Скрываем контейнер превью
        var previewContainer = document.getElementById("previewContainer");
        previewContainer.style.display = "none";
    }
</script>
